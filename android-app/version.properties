# Single source of truth for the launcher APK version.
# tools/ci/bump_version.py advances it; android-app/app/build.gradle.kts reads it
# and tools/app/app_inputs.py hashes it into the packaged app contract.
versionCode=7
versionName=0.4.3
